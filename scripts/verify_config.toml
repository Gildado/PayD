# ──────────────────────────────────────────────────────────────────────────────
# verify_config.toml — Expected contract parameters for verification.
#
# Each `[contracts.<name>]` block defines the checks the verify_contract.sh
# script performs.  Values are grouped by network key inside `[network.<label>]`
# so the same config file works for testnet and mainnet.
#
# Fields:
#   wasm_hash    — hex SHA-256 of the WASM blob (set after a known-good build).
#                  Leave empty ("") to skip WASM-hash comparison (you can still
#                  verify source → WASM reproducibility without pinning a hash).
#   contract_id  — on-chain contract ID (C… string).
#   admin        — expected admin Address (G… string).  The script invokes
#                  `admin()` or reads the DataKey::Admin ledger entry.
#   extra_state  — table of additional storage entries to check, keyed by
#                  the storage symbol name.  Value is the expected JSON/XDR.
# ──────────────────────────────────────────────────────────────────────────────

[network.testnet]
rpc-url = "https://soroban-testnet.stellar.org"
network-passphrase = "Test SDF Network ; September 2015"

[network.mainnet]
rpc-url = "https://soroban-mainnet.stellar.org"
network-passphrase = "Public Global Stellar Network ; September 2015"

# ── Network-specific deployments ──────────────────────────────────────────────
#
# Fill in contract IDs, admin addresses, and any extra init-parameter values
# for each network.  Example:
#
#   [contracts.cross_asset_payment.testnet]
#   contract_id = "CCJZ5FPN5F2Y5H3W4X5Y6Z7A8B9C0D1E2F3G4H5I6J7K8L9M0N1O2P3Q4R5"
#   admin = "GBAZ5FPN5F2Y5H3W4X5Y6Z7A8B9C0D1E2F3G4H5I6J7K8L9M0N1O2P3Q4R5"
#
#   [contracts.vesting_escrow.testnet]
#   contract_id = "CDE..."
#   admin = "GABC..."
#   funder = "GABC..."
#   beneficiary = "GABC..."
#   token = "CDEF..."
#   start_time = 1234567890
#   cliff_seconds = 0
#   duration_seconds = 31536000
#   amount = 1000000000
#   clawback_admin = "GABC..."


# ── Contract configurations ───────────────────────────────────────────────────

# Environment-agnostic build + hash comparisons (no network RPC required).
[contracts.bulk_payment]
# SHA-256 of the release-optimized WASM (verify with: stellar contract info hash --wasm target/wasm32-unknown-unknown/release/bulk_payment.wasm)
wasm_hash = "8939e87db2666dbd527b3ef7106cf9cfc5d0391b050e96aa6d9421d1c07daaba"
[contracts.bulk_payment.params]
init_fn = "initialize"
admin_param = "--admin"
extra_init_args = []

[contracts.cross_asset_payment]
wasm_hash = "12f45858760f723bb197011c138261600e82d9b31716452bfe85da1ead360638"
[contracts.cross_asset_payment.params]
init_fn = "init"
admin_param = "--admin"
extra_init_args = []

[contracts.milestone_escrow]
wasm_hash = "dbe174129015440a5f7180fc19cf5c96f64d813c95fe3025b7e59164b0c6739f"
[contracts.milestone_escrow.params]
init_fn = "initialize"
admin_param = "--admin"
extra_init_args = []

[contracts.orgusd]
wasm_hash = "457486decbc7578c28681cee218c932c12975e25baa4faeed6b055fa960d1371"
[contracts.orgusd.params]
init_fn = "initialize"
admin_param = "--admin"
extra_init_args = []

[contracts.revenue_split]
wasm_hash = "83b1c9dd3ffdc9a9a0dbb980e33c95ecade8e73f8cf38a3bf9bd445b0a6211c3"
[contracts.revenue_split.params]
init_fn = "init"
admin_param = "--admin"
extra_init_args = []
# Note: revenue_split also takes --shares (Vec<RecipientShare>) during init.
# That parameter is verified by querying the on-chain Recipients storage entry.

[contracts.vesting_escrow]
wasm_hash = "4373ccdf426e91ad315769ee1643bef1c735bfca524f3d88b6927c6eaba70d27"
[contracts.vesting_escrow.params]
init_fn = "initialize"
admin_param = "--admin"
extra_init_args = ["--funder", "--beneficiary", "--token", "--start_time", "--cliff_seconds", "--duration_seconds", "--amount", "--clawback_admin"]

[contracts.asset_path_payment]
wasm_hash = "0b7dcd8a44f6b8193513703260b0b2252fc1c8fd43e941f4fe20c3aa059be0e1"
[contracts.asset_path_payment.params]
init_fn = "init"
admin_param = "--admin"
extra_init_args = []

[contracts.smart_wallet]
wasm_hash = "0f28219f052cd18ba731405d993c49e7d97e94ba7195f194e79ff27c285b678e"
[contracts.smart_wallet.params]
init_fn = "init"
admin_param = ""
extra_init_args = ["--signers", "--threshold"]
